# Socket.io sandbox

### How to run

- `cd server`
- `npm i`
- `node server.js`
- `cd ../client`
- `pod install`
- open `SocketIOSandbox.xcworkspace` with Xcode 11+
- build `SocketIOSandbox` target
- run `Sandbox.playground`
